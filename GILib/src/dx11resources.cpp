#include "dx11resources.h"

using namespace gi_lib;
using namespace gi_lib::dx11;

shared_ptr<Resource> DX11Resources::Load(const wstring & path, const std::type_index & type_index){

	return shared_ptr<Resource>(nullptr);

}